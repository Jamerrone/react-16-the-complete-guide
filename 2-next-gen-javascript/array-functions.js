// This is just a small example, most array functions work the same way.
// While the array functions are not 'next-gen JavaScript' they are very important.
// You can find every array function here: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array
const numbers = [1, 2, 3, 4, 5];
const doubleNumbers = numbers.map(num => num * 2);

console.log(numbers);
console.log(doubleNumbers);
